class Solution
{
public:
    
    vector<int> getTable(int N)
    {
        int ans = 1;
        vector<int> result;
        
        for (int i = 1; i <= 10; i++)
        {
            ans = N * i;
        
            result.push_back(ans);
        }
    
        return result;
    }
}
